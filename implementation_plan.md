# [구현 계획서] 전국 어린이 보호구역 데이터 기반 안심 보행 지도 앱 (SafeKids Zone)

---

## 1. 프로젝트 개요
`전국어린이보호구역표준데이터.csv` 데이터를 기반으로 초등학생 자녀와 학부모를 위한 **"어린이 맞춤형 스쿨존 등하교 안심 보행 웹 앱"** 프로토타입을 구축하고 실행합니다.

---

## 2. 모듈별 파일 구조

```
c:\Users\CSE026\Desktop\AI2026_kid\
├── index.html                  # 싱글 페이지 애플리케이션 (SPA) 레이아웃 및 뷰
├── css/
│   └── styles.css              # 테마(어린이/학부모) 디자인 시스템 및 UI 스타일
├── js/
│   ├── csvParser.js            # PapaParse + EUC-KR CSV 파서 & 데이터 검증
│   ├── mapEngine.js            # Leaflet.js 기반 마커 클러스터링 및 지도 인터랙션
│   ├── kidMode.js              # 어린이 UI 감지, 위치 시뮬레이터, 이탈 경고음 & 모달
│   ├── parentMode.js           # 학부모 대시보드, 위험구역(CCTV N) 목록 & 지도 이동
│   ├── questEngine.js          # 등하교 퀘스트, 뱃지 수집 & 포인트 적립
│   └── app.js                  # 메인 데이터 로더 & 모드 전환 상태 관리자
├── PRD.md                      # 제품 요구사항 정의서
├── implementation_plan.md      # 구현 계획서 (현재 파일)
└── 전국어린이보호구역표준데이터.csv # 원본 어린이보호구역 데이터셋
```

---

## 3. 세부 구현 기능

### 3.1 CSV 파서 (`js/csvParser.js`)
- EUC-KR 인코딩의 CSV 파일 읽기 (`FileReader` + `TextDecoder('euc-kr')` + `PapaParse`)
- `위도` (33~39), `경도` (124~132) 유효성 검사 및 정제
- `CCTV설치여부` ('Y' / 'N'), `CCTV설치대수`, `시설종류`, `소재지도로명주소` 필드 정규화

### 3.2 지도 엔진 (`js/mapEngine.js`)
- Leaflet.js + Leaflet.markercluster 기반 무제한 스케일링 마커 렌더링
- CCTV 설치('Y') -> 초록색 안심 마커 🛡️
- CCTV 미설치('N') -> 빨간색 주의 마커 ⚠️
- 마커 클릭 시 개별 보호구역 상세 정보 카드가 포함된 Leaflet Popup 제공

### 3.3 사용자 모드 분리
- **어린이 모드 (`js/kidMode.js`)**:
  - 친근한 안전 노란색/초록색 카스케이딩 디자인
  - 실시간 GPS 또는 가상 등하교 이동 시뮬레이터 (스쿨존 진입/이탈 버튼)
  - 300m 스쿨존 반경 내 위치 여부 대형 초록/주황 안전 카드
  - 스쿨존 이탈 시 경고 애니메이션 모달 + Web Audio API 비프/안내음 재생
- **학부모 모드 (`js/parentMode.js`)**:
  - 다크/슬레이트 톤의 정보 중심 대시보드
  - CCTV 미설치(`CCTV설치여부 == 'N'`) 집중 '위험구역 리스트' 사이드바
  - 위험구역 클릭 시 해당 지도 위치로 PanTo 이동 및 마커 하이라이트
  - 스쿨존 수, CCTV 설치율(%), 미설치 구역 통계 카드

### 3.4 스쿨존 안심 퀘스트 (`js/questEngine.js`)
- 등하교 길 안전 보행 성공 시 "오늘의 등교 퀘스트 성공!" 뱃지 획득
- 연속 성공 스트릭 및 포인트(+100pt) 적립 축하 카드

---

## 4. 검증 계획
1. Python / Node 기반 로컬 Web Server 실행 (`http://localhost:8080`)
2. CSV 정상 파싱 (데이터 개수 및 마커 표출 확인)
3. 모드 전환 (어린이 ↔ 학부모) 동작 테스트
4. 스쿨존 이탈 알림 모달 및 효과음 시뮬레이션 테스트
5. 학부모 모드 위험구역 클릭 시 지도 이동 검증
