# [PRD] 어린이 맞춤형 스쿨존 등하교 안심 보행 웹/앱 (SafeKids Zone)

---

## 1. 프로젝트 개요 (Project Overview)

### 1.1 배경 및 목적
- **배경**: 초등학생 자녀의 독립적인 등하교 과정에서 통학로 안전에 대한 학부모의 불안감이 커지고 있으며, 어린이보호구역(스쿨존) 내 사고 예방 및 안전 시설(CCTV 등) 현황 파악의 필요성이 증대됨.
- **목적**: `전국어린이보호구역표준데이터.csv` 데이터를 활용하여 초등학생 자녀에게는 친근하고 직관적인 안전 등하교 가이드를, 학부모에게는 사각지대(CCTV 미설치 구역) 정보와 자녀 동선 관제를 제공하는 **"어린이 맞춤형 스쿨존 등하교 안심 보행 웹/앱 서비스"** MVP를 구축함.

### 1.2 주요 타겟 사용자
1. **어린이 사용자 (초등학생)**: 시각적이고 직관적인 안전 알림과 게이미피케이션(퀘스트/뱃지) 요소가 필요한 자녀.
2. **학부모 사용자**: 전체 스쿨존 현황, CCTV 설치 유무, 통학로 위험구역 목록 및 자녀 위치를 한눈에 파악하고자 하는 학부모.

---

## 2. 데이터 사양 및 처리 방안 (Data Specifications)

### 2.1 사용 데이터 원천
- **파일명**: `전국어린이보호구역표준데이터.csv`
- **인코딩**: EUC-KR / CP949 (파싱 시 UTF-8 변환 처리)

### 2.2 주요 컬럼 스펙
| 컬럼명 | 타입 | 설명 및 활용 방안 |
| :--- | :--- | :--- |
| `시설종류` | String | 어린이집, 초등학교, 유치원, 특수학교 등 필터링 기준 |
| `대상시설명` | String | 보호구역 명칭 (예: 서울○○초등학교) |
| `소재지도로명주소` | String | 시설 주소 정보 (검색 및 상세 카드 표시) |
| `위도` (Latitude) | Number | 지도 마커 렌더링 좌표 (유효범위: 33.0 ~ 39.0) |
| `경도` (Longitude) | Number | 지도 마커 렌더링 좌표 (유효범위: 124.0 ~ 132.0) |
| `CCTV설치여부` | String ('Y'/'N') | **안심 구역('Y')** vs **주의 구역('N')** 마커 색상 구분 기준 |
| `CCTV설치대수` | Number | 학부모 모드 상세 팝업 및 통계 표출 |
| `보호구역도로폭` | String | 도로폭 정보 표출 |

### 2.3 데이터 파싱 및 정제 규칙
1. **유효성 검사**: `위도` 또는 `경도` 값이 없거나(null/empty), 숫자가 아닌 데이터, 한국 반경을 벗어난 데이터는 제외.
2. **인코딩 대응**: `PapaParse` 및 `TextDecoder`를 활용하여 EUC-KR 한글 깨짐 없이 정상 파싱.
3. **CCTV 상태 필터링**: `CCTV설치여부`가 null인 경우 'N'으로 기본 처리.

---

## 3. 핵심 기능 요구사항 (Key Features & Functional Requirements)

### 3.1 CSV 데이터 기반 지도 시각화 (Map & Visualisation)
- **Leaflet.js + CartoDB Voyager 동화풍 지도 렌더링**
  - **어린이 모드 지도 타일**: 기존의 딱딱하고 사실적인 일반 지도 대신, 아이들이 좋아하는 파스텔 톤의 동화풍 지도 타일 (**CartoDB Voyager**) 및 채도 커스텀 필터를 적용.
  - **학부모 모드 지도 타일**: 대시보드 모니터링에 적합한 다크 모드 슬레이트 타일 (**CartoDB Dark Matter**)로 자동 전환.
  - 마커 구분:
    - **초록색 안심 쉴드 마커 (Green Shield Pin 🛡️)**: `CCTV설치여부 == 'Y'` (CCTV 설치대수 Badge 표시)
    - **빨간색 주의 마커 (Red Warning Pin ⚠️)**: `CCTV설치여부 == 'N'` (경고 아이콘 표시)
- **인터랙티브 마커 & Popup UI**: 마커 클릭 시 시설명, 주소, 시설종류, CCTV 설치대수, 도로폭 정보 카드 표출.
- **검색 및 필터링 기능**: 시설명 / 도로명 주소 키워드 검색.

---

### 3.2 사용자 모드 분리 (Dual-Mode System)

```
                     [ 모드 전환 토글 ]
                            │
            ┌───────────────┴───────────────┐
            ▼                               ▼
    【 어린이 모드 (Kid Mode) 】     【 학부모 모드 (Parent Mode) 】
    - 아기자기한 노란/초록 UI        - 정보 중심 대시보드 & 지도
    - 대형 스쿨존 안전 카드          - CCTV 분포 & 위험구역 리스트
    - GPS 스쿨존 이탈 경고 팝업       - 자녀 실시간 가상 위치 모니터링
    - 등하교 안심 퀘스트 & 뱃지       - 스쿨존 통계 요약 (설치율 등)
```

#### A. 어린이 모드 (Kid Mode UI/UX)
- **Visual Style**: 노란색/초록색 기반의 밝고 친근한 둥근 카드 디자인, 가독성 높은 대형 폰트.
- **현재 위치 & 스쿨존 범위 감지**:
  - 사용자 위치(GPS 또는 가상 등하교 이동 시뮬레이션) 추적.
  - **스쿨존 내부 (안전)**: "현재 안전한 스쿨존 안이에요! 🛡️" (초록색 대형 스테이터스 카드)
  - **스쿨존 외부/경계 이탈 (주의)**: "안전구역을 벗어났어요! 차조심하세요! ⚠️" (주황/빨강 경고 카드)
- **이탈 경고 팝업 & 효과음**:
  - 스쿨존 이탈 이벤트 발생 시 풀스크린 경고 팝업 애니메이션 실행.
  - Web Audio API 기반 "띠링! 주의하세요" 안내 알림음 재생 시뮬레이션.

#### B. 학부모 모드 (Parent Mode UI/UX)
- **Visual Style**: 차분하고 직관적인 신뢰감 중심의 모던 대시보드 (Dark/Light Slate 톤).
- **전체 지도 & 자녀 위치 모니터링**:
  - 자녀의 실시간 이동 동선 마커 표출.
  - 주변 스쿨존 및 CCTV 설치대수 분포 파악.
- **'위험구역 리스트' 탭/사이드바**:
  - CCTV 미설치 지역(`CCTV설치여부 == 'N'`)을 한눈에 모아보는 전용 목록 제공.
  - 리스트 아이템 클릭 시 해당 위치로 지도가 부드럽게 이동(PanTo)하며 마커 하이라이트.
- **스쿨존 요약 데이터 통계**:
  - 전체 스쿨존 수, CCTV 설치율(%), 미설치 구역 수 등 요약 대시보드 메트릭 카드.

---

### 3.3 스쿨존 안심 퀘스트 & 포인트 게이지바 & 12종 동물 도감 팝업 (Gamification)
- **등하교 퀘스트 & 포인트 게이지바 (Milestone Gauge Bar)**:
  - "오늘의 안전 등교 퀘스트" 성공 시 **+100pt 적립**.
  - 퀘스트 카드 하단에 **`100pt 🎁` | `200pt 🎁` | `300pt 🎁` | `400pt 🎁` | `500pt 🎁`** 포인트 마일스톤 게이지 바 제공.
  - 포인트가 해당 마일스톤에 도달하면 보물상자 아이콘이 톡톡 튀며 활성화되며, **터치 시 보물상자가 즉시 개봉**되어 동물 스티커를 획득합니다.
- **깔끔한 12종 동물 스티커 도감 팝업창 (Collection Modal)**:
  - 메인 패널에는 `[🐾 내 동물 스티커 도감 구경하기 (3/12)]` 버튼을 배치하여 깔끔한 UX 유지.
  - 버튼 클릭 시 대형 **동물 스티커 도감 팝업창**이 오픈되며 수집한 귀여운 동물 12종을 감상할 수 있습니다.
  - **12종 귀여운 동물 라인업**: 아기 고양이 🐱, 복실 멍뭉이 🐶, 삐약이 🐥, 깡총 토끼 🐰, 동글 햄스터 🐹, 아기 판다 🐼, 보노 수달 🦦, 아기 여우 🦊, 몽실 곰돌이 🐻, 포근 코알라 🐨, 아기 펭귄 🐧, 무지개 유니콘 🦄
  - 수집된 스티커 클릭 시 보행 안전 팁 팝업 제공.

---

## 4. UI/UX 디자인 가이드라인 (Design Guidelines)

| **항목** | **어린이 모드 (Kid Mode - 심플 최적화)** | **학부모 모드 (Parent Mode)** |
| :--- | :--- | :--- |
| **디자인 콘셉트** | 직관적이고 커다란 카드, 한눈에 들어오는 가독성, 복잡한 통계/검색창 제거 | 슬레이트 다크 대시보드, 전문 정보 중심 데이터 표출 |
| **메인 컬러** | `#FFD166` (안전 노랑), `#06D6A0` (초록) | `#1E293B` (슬레이트), `#3B82F6` (신뢰 파랑) |
| **포인트 컬러** | `#EF476F` (경고 분홍), `#7E22CE` (보물 퍼플) | `#EF476F` (위험구역 Red), `#10B981` (CCTV Safety) |
| **버튼 & 카드** | 큼직하고 폭신한 라운딩 (24px), 시원한 폰트 (1.1rem~1.5rem) | 슬림 라운딩 (12px~14px), 정갈한 샌세리프 |
| **검색창 / 상단바** | 검색창 숨김 (어린이 눈높이 맞춤 직관적 UI) | 주소/학교명 통합 키워드 검색창 제공 |

---

## 5. 기술 아키텍처 및 구현 사양 (Technical Specs)

```
[ frontend/ ]
├── index.html                  # 메인 싱글 페이지 애플리케이션 (SPA)
├── css/
│   └── styles.css              # Vanilla CSS 디자인 시스템 (어린이/학부모 테마 CSS 변수)
├── js/
│   ├── app.js                  # 메인 앱 진입점 및 모드 관리자
│   ├── csvParser.js            # PapaParse 기반 EUC-KR CSV 파서 및 데이터 정제
│   ├── mapEngine.js            # Leaflet.js 지도, 커스텀 마커, 클러스터링, 반경 계산
│   ├── kidMode.js              # 어린이 UI, GPS/위치 시뮬레이터, 이탈 경고 및 Sound FX
│   ├── parentMode.js           # 학부모 대시보드, 위험구역 리스트 및 통계
│   └── questEngine.js          # 게이미피케이션 (등하교 퀘스트, 뱃지, 포인트)
└── 전국어린이보호구역표준데이터.csv # 원본 표준 데이터셋
```

- **라이브러리 (CDN)**:
  - **Leaflet.js (v1.9.4)**: 지도 렌더링
  - **Leaflet.markercluster**: 다량 마커 성능 최적화
  - **PapaParse (v5.4.1)**: CSV 파싱
  - **FontAwesome / Lucide Icons**: 아이콘 팩

---

## 6. 개발 로드맵 (Development Roadmap)

1. **Step 1 (완료)**: `PRD.md` 작성 및 요구사항 정의.
2. **Step 2**: 기본 파일 구조 생성 (`index.html`, `styles.css`, JS 모듈).
3. **Step 3**: `csvParser.js` 구현하여 CSV 데이터 정상 파싱 및 유효성 검증.
4. **Step 4**: Leaflet 지도 초기화 및 CCTV 설치여부별 마커 시각화 (`mapEngine.js`).
5. **Step 5**: 어린이 모드 개발 (스쿨존 감지, 이탈 경고 팝업, 알림음 효과) & 학부모 모드 개발 (위험구역 목록, 대시보드).
6. **Step 6**: 등하교 안심 퀘스트 게이미피케이션 구현 & UI 최종 Polish.
